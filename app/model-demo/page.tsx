import Link from 'next/link';
export default function ModelDemo() { return <div className="container"><section className="page-hero"><div className="eyebrow">Model demo / RGB ResNet50</div><h1>A model surface ready for a real connection.</h1><p>The RGB experiment uses transfer learning with ResNet50 and a ten-class land-cover head. This page documents the contract without pretending inference is live.</p></section><div className="empty-state"><div className="empty-symbol">TS</div><div><h2>Model inference coming soon.</h2><p>A future POST /api/predict endpoint will accept RGB image data and return a predicted class, confidence, ten class probabilities, and optional Grad-CAM data. Keep model loading and preprocessing in the Python backend, separate from this frontend.</p><div className="button-row"><Link className="button" href="/upload">Open upload surface <span>→</span></Link></div></div></div><section className="section"><div className="feature-grid"><div className="data-card"><span className="mono">INPUT</span><h3>JPG / JPEG / PNG</h3><p>RGB image uploads only. Multispectral tensors require a separate, explicit pathway.</p></div><div className="data-card"><span className="mono">MODEL</span><h3>ResNet50</h3><p>Transfer-learning baseline trained on the EuroSAT ten-class taxonomy.</p></div><div className="data-card"><span className="mono">OUTPUT</span><h3>Structured JSON</h3><p>Prediction, confidence, probabilities, and explanation metadata when available.</p></div></div></section></div>; }