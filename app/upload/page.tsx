'use client';
import { useState } from 'react';
import Link from 'next/link';
export default function UploadPage() {
  const [fileName, setFileName] = useState('');
  return <div className="container"><section className="page-hero"><div className="eyebrow">Try the model / RGB only</div><h1>Bring an image. Keep the result honest.</h1><p>Upload a JPG, JPEG, or PNG preview for the future RGB ResNet50 endpoint. The model is not connected yet, so this page will not invent a prediction.</p></section><section className="upload-box"><div className="dropzone"><div className="eyebrow">Image input</div><h2>{fileName || 'Choose an RGB image'}</h2><p className="section-copy">Preview and validation will appear here before analysis.</p><input aria-label="Upload an image" type="file" accept=".jpg,.jpeg,.png,image/jpeg,image/png" onChange={event => setFileName(event.target.files?.[0]?.name || '')} /><div className="button-row"><button className="button" type="button" disabled>Analyze image <span>→</span></button></div></div><div className="warning"><strong>Experimental scope:</strong> ordinary RGB uploads may be outside the EuroSAT training distribution. This interface is deliberately limited to the RGB model and will never send an RGB upload to the 13-band multispectral model.</div></section><div className="feature-grid"><div className="data-card"><span className="mono">PENDING / 01</span><h3>Model inference coming soon</h3><p>POST /api/predict is reserved for a separate PyTorch inference backend. No checkpoint or backend is included in this repository.</p></div><div className="data-card"><span className="mono">PENDING / 02</span><h3>Expected response</h3><p>Predicted class, confidence, all 10 class probabilities, and a clear success or error state.</p></div><div className="data-card"><span className="mono">PENDING / 03</span><h3>Explainability</h3><p>Grad-CAM will only appear when a real inference response includes a real explanation artifact.</p></div></div><section className="section"><div className="note-box"><div className="eyebrow">Need the model context?</div><h2>Read the current experiment first.</h2><p>The supplied baseline used a ResNet50 transfer-learning setup with only the final classification layer trained.</p><Link className="button secondary" href="/methodology">Open methodology <span>→</span></Link></div></section></div>;
}