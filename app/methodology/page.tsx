export default function Methodology() { return <div className="container"><section className="page-hero"><div className="eyebrow">Methodology / pipeline</div><h1>From pixels to a measured comparison.</h1><p>The current research path keeps data preparation, model training, evaluation, and future temporal analysis visible as separate steps.</p></section><div className="pipeline">{['EuroSAT','Preprocess','Split','ResNet50','Classify','Evaluate','Compare / change'].map((step, index) => <div className="pipeline-step" key={step}><span>0{index + 1}</span><strong>{step}</strong></div>)}</div><section className="section"><div className="feature-grid"><div className="data-card"><span className="mono">01 / SPLIT</span><h3>18,900 / 4,050 / 4,050</h3><p>Train, validation, and test partitions. The test set remains untouched for model tuning.</p></div><div className="data-card"><span className="mono">02 / TRAINING</span><h3>Adam · 10 epochs</h3><p>Both experiments use a 0.001 learning rate and CrossEntropyLoss. Only the final classification layer was trained.</p></div><div className="data-card"><span className="mono">03 / MULTISPECTRAL</span><h3>Modified input layer</h3><p>The 13-band baseline modifies the first convolutional layer and applies simple input scaling. This limits how broadly the comparison can be generalized.</p></div></div></section></div>; }